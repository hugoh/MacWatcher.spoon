name: Tag

permissions:
  contents: write
  actions: write

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main

concurrency:
  group: tag
  cancel-in-progress: false

jobs:
  tag:
    uses: hugoh/spoon-tools/.github/workflows/spoon-tag.yml@00ffd41dd5a3ec8dd556e29ca219151860a9bae8 # v1.2.2
